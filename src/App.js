import * as THREE from 'three';
import './App.css';
import { Canvas, extend, useFrame, useLoader, useThree } from 'react-three-fiber';

function App() {
  return (
    <div className="anim">
     
    </div>
  );
}

export default App;
